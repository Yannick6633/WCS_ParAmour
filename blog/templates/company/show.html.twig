{% extends 'base.html.twig' %}

{% block title %}Company{% endblock %}

{% block body %}
    <h1>Company</h1>
<div class="container">
    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ company.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ company.content }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ company.price }}</td>
            </tr>
            <tr>
                <th>Picture1</th>
                <td>
                    <img src="{{ company.picture1 }}" alt="Card-img-top" style="width: 100%; height: auto;">
                </td>
            </tr>
            <tr>
                <th>Picture2</th>
                <td>
                    <img src="{{ company.picture2 }}" alt="Card-img-top" style="width: 100%; height: auto;">
                </td>
            </tr>
            <tr>
                <th>Picture3</th>
                <td>
                    <img src="{{ company.picture3 }}" alt="Card-img-top" style="width: 100%; height: auto;">
                </td>
            </tr>
        </tbody>
    </table>
</div>

    <div class="container">
        <div class="row">
    <a  class="btn btn-outline-warning btn-sm" href="{{ path('company_index') }}">back to list</a>

    <a  class="btn btn-outline-success btn-sm" href="{{ path('company_edit', {'id': company.id}) }}">edit</a>

    {{ include('company/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}

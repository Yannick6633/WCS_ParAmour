<div class="container justify-content-around">
    <p>{% if app.user %}
            Bonjour {{ app.user.email }} !
            <a class="btn btn-link btn-sm" href="{{ path('app_logout') }}"><i class="fas fa-user"></i> Déconnexion</a>
        {% else %}
            <a class="btn btn-link btn-sm" href="{{ path('app_login') }}"><i class="fas fa-user"></i> Connexion</a>
        {% endif %}</p>
</div>